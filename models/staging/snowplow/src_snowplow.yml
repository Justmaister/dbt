version: 2 

sources:
    - name: snowplow
      #database: raw
      tables:
        - name: events
          loaded_at_field: collector_timestamp
          freshness:
            error_after: {count: 1, period: hour}